#include<iostream>
#include<string>
#include<vector>
using namespace std;

void solution(string str, string output, vector<string> &ans , int index)
{
    // base case
    if (index >= str.length())
    {
        ans.push_back(str);
        return;
    }

    for (int i = 0; i < str.length(); i++)
    {
        
    }
    
}

int main()
{
    string str = "abc";
    string output;
    vector<string> ans;
    int index = 0;

    

    return 0;
}